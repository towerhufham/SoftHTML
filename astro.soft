div.post-preview
  div.sm:w-20 md:w-32
    [LINKS] div.post-preview__date
    
    [TEXT] div.flex-1


#in [LINKS]
span { new Date(post.date).getDate() }
post-preview__date__day

span { `${getMonthName(post.date)} ${new Date(post.date).getFullYear()}` }
post-preview__date__month-n-year


#in [TEXT]
h4
post-preview__title
a {post.title}
@href {url}
@title {post.title}

p {post.description}
post-preview__desc